# Fields

```ts
  // Extension Types instruction For Creating new Hook Form Field Component and interfaces

  - HookFormFieldType<T extends FieldValues = FieldValues> = T;

  - HookFormFieldProps<T extends FieldTypes, OriginTypes, OptionalTypes>

  - HookFormFieldComponent<extension props from FormFieldProps>

```
